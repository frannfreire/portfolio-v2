---
---

<section id="contacto">
	<div class="section-header">
		<span class="section-index">05</span>
		<h2>CONTACTO<span class="accent">.</span></h2>
	</div>
	
	<div class="contact-content">
		<p class="cta-text">
			¿Proyecto en mente? ¿Automatización? ¿IA para tu negocio?
		</p>
		
		<div class="contact-grid">
			<a href="mailto:frannfreire@gmail.com" class="contact-item">
				<span class="label">EMAIL</span>
				<span class="value">frannfreire@gmail.com</span>
			</a>
			<a href="https://t.me/frannfreire" target="_blank" class="contact-item">
				<span class="label">TELEGRAM</span>
				<span class="value">@frannfreire</span>
			</a>
			<a href="https://github.com/frannfreire" target="_blank" class="contact-item">
				<span class="label">GITHUB</span>
				<span class="value">frannfreire ↗</span>
			</a>
			<a href="https://linkedin.com/in/frannfreire" target="_blank" class="contact-item">
				<span class="label">LINKEDIN</span>
				<span class="value">frannfreire ↗</span>
			</a>
		</div>
	</div>
</section>

<footer>
	<div class="footer-content">
		<span class="copyright">© 2025 FRANCISCO FREIRE</span>
		<span class="made-with">HECHO CON ASTRO</span>
	</div>
</footer>

<style>
	section {
		padding: 6rem 0;
	}

	.section-header {
		display: flex;
		align-items: center;
		gap: 2rem;
		margin-bottom: 4rem;
	}

	.section-index {
		font-family: 'Anton', sans-serif;
		font-size: 1rem;
		color: var(--accent);
		letter-spacing: 0.1em;
	}

	h2 {
		font-size: clamp(3rem, 8vw, 6rem);
	}

	.accent {
		color: var(--accent);
	}

	.cta-text {
		font-size: 1.25rem;
		color: var(--text-secondary);
		margin-bottom: 3rem;
		max-width: 500px;
	}

	.contact-grid {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 1rem;
	}

	.contact-item {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
		padding: 2rem;
		background: var(--bg-card);
		border: 1px solid var(--border);
		transition: all 0.3s;
		color: var(--text-primary);
	}

	.contact-item:hover {
		border-color: var(--accent);
		transform: translateX(8px);
	}

	.label {
		font-size: 0.625rem;
		letter-spacing: 0.2em;
		color: var(--text-secondary);
	}

	.value {
		font-family: 'Anton', sans-serif;
		font-size: 1.125rem;
		letter-spacing: 0.02em;
	}

	.contact-item:hover .value {
		color: var(--accent);
	}

	footer {
		padding: 4rem 0;
		border-top: 1px solid var(--border);
	}

	.footer-content {
		display: flex;
		justify-content: space-between;
		align-items: center;
		font-size: 0.75rem;
		letter-spacing: 0.1em;
		color: var(--text-secondary);
	}

	@media (max-width: 768px) {
		.contact-grid {
			grid-template-columns: 1fr;
		}

		.footer-content {
			flex-direction: column;
			gap: 1rem;
			text-align: center;
		}
	}
</style>
